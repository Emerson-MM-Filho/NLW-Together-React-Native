import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 64, 
    width: 64,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: 20,
    borderRadius: 8,
  }, avatar: {
    width:62,
    height: 62,
    borderRadius: 8,
  }
})